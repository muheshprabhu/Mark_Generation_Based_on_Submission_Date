import { Routes } from '@angular/router';
import { MarkGeneratorComponent } from './mark-generator/mark-generator';

export const routes: Routes = [
  { path: '', component: MarkGeneratorComponent }
];
